# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PrivatbankATMMap' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PrivatbankATMMap
  pod 'Alamofire', '~> 4.5'
  pod 'SwiftyJSON'
  pod 'DropDown', :path => './DropDown/' # DropDown is located on the disk. I had to rewrite it a bit for the correct use.

end
